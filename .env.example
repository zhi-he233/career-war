# Change game.example.com to your real production domain before deployment.
NODE_ENV=production
PORT=3001
CLIENT_ORIGIN=https://game.example.com

# First VPS version does not need DATABASE_URL.
# Same-origin production deployment does not need VITE_SERVER_URL.
